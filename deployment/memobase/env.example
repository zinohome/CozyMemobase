# Database Configuration
DATABASE_USER=memobase
DATABASE_PASSWORD=memobase123
DATABASE_NAME=memobase
DATABASE_EXPORT_PORT=5432
DATABASE_LOCATION=./data/db

# Redis Configuration
REDIS_EXPORT_PORT=6379
REDIS_LOCATION=./data/redis
REDIS_PASSWORD=redis123

# Memobase API Configuration
# ACCESS_TOKEN: API访问令牌，用于Bearer认证。如果未设置，将允许所有请求（仅用于开发）
ACCESS_TOKEN=secret
# PROJECT_ID: 项目ID，用于多租户隔离
PROJECT_ID=default
# API_HOSTS: API允许的主机列表，逗号分隔。用于CORS配置和OpenAPI文档
API_HOSTS=https://api.memobase.dev,https://api.memobase.cn
# USE_CORS: 是否启用CORS（true/false）
USE_CORS=true
# API_EXPORT_PORT: API服务对外暴露的端口
API_EXPORT_PORT=8019

# LLM Configuration (可选，也可以通过 config.yaml 配置)
# MEMOBASE_LLM_API_KEY: LLM API密钥（OpenAI或其他兼容服务）
# 注意：如果未设置此环境变量，需要在 /data/memobase/config.yaml 中配置 llm_api_key
# MEMOBASE_LLM_API_KEY=sk-xxx
# MEMOBASE_LLM_BASE_URL: LLM API基础URL（可选，默认使用OpenAI官方API）
# MEMOBASE_LLM_BASE_URL=https://api.openai.com/v1

# Embedding Configuration (可选，也可以通过 config.yaml 配置)
# MEMOBASE_EMBEDDING_API_KEY: Embedding API密钥（可选，如果未设置且使用OpenAI，将使用LLM API密钥）
# MEMOBASE_EMBEDDING_BASE_URL: Embedding API基础URL（可选）
# MEMOBASE_EMBEDDING_PROVIDER: Embedding提供商，可选值: openai, jina, ollama（默认: openai）
# MEMOBASE_EMBEDDING_PROVIDER=openai

# Optional: Admin Configuration
# ADMIN_URL: 管理员API URL（可选）
# ADMIN_TOKEN: 管理员令牌（可选）

# Optional: Logging Configuration
# LOG_FORMAT: 日志格式，可选值: plain, json（默认: plain）
# LOG_FORMAT=plain

# Optional: Usage Limits
# USAGE_TOKEN_LIMIT_ACTIVE: Active项目级别的token限制
# USAGE_TOKEN_LIMIT_PRO: Pro项目级别的token限制
# USAGE_TOKEN_LIMIT_ULTRA: Ultra项目级别的token限制

# Config File Path
# 注意：大模型配置主要通过 /data/memobase/config.yaml 文件配置
# 环境变量 MEMOBASE_* 可以覆盖 config.yaml 中的配置
CONFIG_FILE=./config.yaml

